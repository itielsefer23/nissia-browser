# nissia browser — agent guide (Claude Code)

Claude Code reads `CLAUDE.md`, not `AGENTS.md`. To keep a single source of truth,
this file imports the cross-tool guide. The full instructions for driving the
`nissia` CLI (commands, token economy, batch, safety) live in AGENTS.md.

@AGENTS.md

For the richer, mode-driven workflow (Search / Navigate / Agent, browser
selection, operating forms like a human), Claude Code also ships the
`/nissia-browser` skill in `.claude/skills/nissia-browser/SKILL.md`.
